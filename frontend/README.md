# منصة معيار — واجهة النظام (React + Tailwind)

نموذج تشغيلي كامل للمنصة الوطنية لاختبارات التربة والطرق (الإدارة العامة لكود البناء السعودي — وزارة البلديات والإسكان)، من تسجيل الدخول حتى اعتماد تقرير الدراسة الجيوتقنية، مبني على BRD v2.0 ومصفوفة الصلاحيات، مع تصحيحات نتائج البحث في الكود السعودي (SBC 303/301) وضوابط NCA ECC-2:2024.

- **عربي فقط · وضع فاتح فقط · RTL** — لا يوجد وضع داكن ولا لغة ثانية.
- Vite 8 · React 19 · TypeScript · Tailwind 4 · Zustand · React Router 7 · Recharts · lucide-react
- بدون خادم: كل البيانات في `src/lib/mock.ts` (مولّد ببذرة ثابتة + قصة عرض مكتوبة يدوياً) وآلة الحالات في `src/lib/store.ts`.

## التشغيل

```bash
npm install
npm run dev        # http://localhost:5173
npm run build && npm run preview   # http://localhost:4173
```

## نظام التصميم

| العنصر | المصدر |
|---|---|
| الألوان الرئيسية | الأخضر الداكن من شعار معيار الرسمي — `#1E6355` (600) / `#165449` (الشريط الجانبي) / `#104840` (الشريط العلوي) + ليموني وزارة الإسكان `#88B925` |
| المحايدات، الدلالية، نصف الأقطار | **كود المنصات** (هيئة الحكومة الرقمية) — `#FCFCFD…#0C111B`، نجاح `#069454`، تحذير `#DC6803`، خطأ `#D92C20`، معلومات `#156FEE` |
| الخط | IBM Plex Sans Arabic (كود المنصات) |
| لوحة المخططات | `#12A39B · #2A78D6 · #E06A2E · #8250C8 · #B8860B` (مُتحقق منها لعمى الألوان) |
| الشعارات | شعار معيار الرسمي من `../brand/` → `public/brand/logo.png` / `logo-vertical.png` / `mark.png` (+ نسخ `-on-dark` للأسطح الداكنة)، أيقونات `public/favicon-32.png` / `icon-192.png` / `apple-touch-icon.png`؛ `public/brand/momrah.svg` (شعار الوزارة الرسمي — مُنزّل)، `public/brand/ido.svg` |

**تحديث الشعار:** ضع النسخ الجديدة في `D:\pre-sales\Miyarrand\` (`logo-default.png`, `logo-vertical.png`, `app-icon.png`) ثم شغّل `python scripts/brand-assets.py` (يقصّ الهوامش، يولّد نسخة فاتحة للأسطح الداكنة، ويولّد الأيقونات).

كل الرموز في `src/index.css` (`@theme`)، والمكوّنات في `src/ds/`:

- `primitives.tsx` — Button / Badge / StatusPill / Kpi (مع sparkline واتجاه) / Section / Field / Input / Select / Tabs / Table / KV / Modal / Drawer / Progress …
- `DataTable.tsx` — جدول عام: بحث، فلاتر، فرز، اختيار الأعمدة، كثافة، ترقيم صفحات، تحديد جماعي، تصدير CSV.
- `charts.tsx` — TimeArea / TimeLine / Bars / RankBars / Donut / Heat فوق Recharts، متوافقة مع RTL.
- `composite.tsx` — PageHeader / FlowSteps / PhaseStepper / Timeline / Countdown / FileTile / PlotMap.
- `Logo.tsx` — MiyarLogo / MiyarMark / MomrahLogo / IdoLogo.

## الدخول التجريبي

شاشة الدخول فيها "دخول سريع" لكل الأدوار. أو أضف `?as=<userId>` لأي رابط:

| المستخدم | الدور | الصفة | userId |
|---|---|---|---|
| أحمد العتيبي | المقاول | المفوّض الرئيسي | `u-cont` |
| سارة الدوسري | المقاول | موظف | `u-cont-emp` |
| محمد السبيعي | المختبر | المفوّض الرئيسي | `u-lab` |
| فيصل القحطاني | المختبر | موظف (مفوَّض على TST-2026-047/046) | `u-lab-emp` |
| نواف الشمري | المختبر | موظف (تفويض غير مباشر بانتظار قبوله) | `u-lab-emp2` |
| خالد العسيري | المكتب الاستشاري | المفوّض الرئيسي | `u-cons` |
| ريم الحربي | المكتب الاستشاري | موظف (تراجع ولا تعتمد) | `u-cons-emp` |
| عبدالله الغامدي | الجهة الإشرافية | قراءة فقط | `u-sup` |
| ياسر علي | مدير النظام | — | `u-admin` |
| رحاب مدخلي | الدعم التقني | — | `u-support` |

مثال: `http://localhost:5173/requests/TST-2026-046/study?as=u-lab`

## الشاشات

| المسار | الشاشة | الأدوار |
|---|---|---|
| `/login` · `/register` | دخول OTP / نفاذ · تسجيل منشأة (واثق → المفوّض → OTP) | الجميع |
| `/` | لوحة معلومات حسب الدور (مؤشرات، مخططات، إجراءات عاجلة، مواعيد، نشاط) — لوحة مستقلة للدعم | الجميع |
| `/requests` · `/requests/new` · `/requests/:id` | قائمة الطلبات (DataTable) · إنشاء طلب · تفاصيل الطلب (خط سير، اختبارات، مهل، مستندات، مالية، حوكمة) | مقاول/مختبر/استشاري/إشراف |
| `/requests/:id/tests/:t/execute` · `…/review` · `…/retest` | تنفيذ الاختبار (عينة + نتائج + تقرير) · مراجعة الاستشاري · طلب إعادة | مختبر · استشاري · مقاول |
| `/approvals` | قائمة اعتماد المخرجات (مستوى الاختبار) مع المهل | استشاري |
| `/requests/:id/study/…` | الدراسة الجيوتقنية: أولية → خطة (محرك SBC 303 Table 2.1) → ميداني (خريطة جسات) → جسة/سجل جسة → معملية → تحليل → تقرير (17 بنداً) | مقاول/مختبر/استشاري |
| `/delegations` | التفويض المباشر/غير المباشر | مختبر · استشاري |
| `/catalog` | قائمة اختبارات المختبر | مختبر |
| `/directory` · `/directory/:id` · `/profile` | دليل المنشآت (عام للزائر أيضاً) · ملف منشأة · إدارة الملف والموظفين | الجميع |
| `/contracts` · `/invoices` · `/results` | العقود (مع درج تفاصيل) · الفواتير (أعمار الذمم) · النتائج والشهادات | حسب الدور |
| `/reports` · `/archive` · `/governance` · `/governance/policies` | التقارير والتحليلات · الأرشيف · سجل التدقيق والامتثال · السياسات | إشراف/إدارة |
| `/admin/*` | القواعد · العناصر المرجعية · قاعدة المعرفة · القوالب · الحسابات · التقييمات | مدير النظام · الدعم |
| `/settings` · `/help` | الحساب والجلسات والأمان · الأدلة والتذاكر والتكاملات | الجميع |

## سيناريوهات جاهزة للعرض

1. **طلب قياسي كامل** — كمقاول: `/requests/new` → اختيار سريع من قائمة المختبر (قيد التصنيف) → 3 نطاقات → إرسال. كمختبر: `/requests/TST-2026-051` → النطاق الأول مختار افتراضياً → قبول (عدّاد 12 ساعة). ثم تنفيذ الاختبار: تسجيل استلام العينة (GPS) → المقاول يؤكد (يبدأ SLA) → إدخال النتائج مقابل الحدود المرجعية ورفع التقرير. كاستشاري: `/approvals` → مراجعة واعتماد/رفض (عدّاد 48 ساعة). رفض → المقاول ينشئ إعادة `TST-…-R1`.
2. **الدراسة الجيوتقنية الجارية** — `TST-2026-046` في المرحلة 3. كمختبر: خريطة الجسات → BH-02 قيد التنفيذ → إضافة طبقات (SPT يحسب N) وعينات → إكمال الجسة → Borehole Log مولّد (مع منحنى SPT-N).
3. **دراسة مكتملة ومعتمدة** — `TST-2026-038` في المرحلة 6: البيانات المعملية (9 عينات) → التحليل (حسابية/تحليلية زلزالية SBC 301/توصيات) → معاينة التقرير معتمدة وموزّعة → المستند في الأرشيف والنتائج.
4. **دراسة من الصفر** — كمقاول: طلب جديد بنوع "دراسة جيوتقنية" → البيانات الأولية (رفع القرار المساحي، استخراج، مساحة مبنية وأدوار) → كاستشاري: مراجعة → المحرك يقترح الجسات وفق **SBC 303 Table 2.1** → تعديل على الخريطة → اعتماد وإرسال للمختبر.
5. **الحوكمة** — كمدير نظام: `/admin/rules` (تعديل مهلة → حدث حرج في `/governance`) · `/admin/knowledge` (إصدارات القاعدة وأثرها) · `/governance` (الامتثال ECC، مصفوفة الصلاحيات).

## ملاحظات فنية

- الساعة مرجعها الوقت الفعلي (`Date.now()`) فتبقى العدّادات حيّة في العرض، والبذرة الثابتة تُبقي البيانات نفسها.
- المسودات (STS09) لا يراها إلا المقاول المنشئ؛ الموظف يرى ما فُوِّض له فقط (`visibleRequests` في `store.ts`).
- `?as=` يسجّل الدخول ثم يُزال من الرابط — للعرض فقط.

## التدقيق التشغيلي

```bash
npx tsx scripts/flow-audit.ts   # يشغّل آلة الحالات الحقيقية عبر رحلة الطلب/التفويض/التقييم/المحرك ويطبع أي خرق لقواعد BRD (المتوقع: 0 findings)
```

## طبقة الأدلة (ما تنتجه المختبرات المعتمدة فعلاً)

مستوحاة من تقرير GSSAAT/سماء المرجعي (ورقة اختبار MTS، صور العينات قبل/بعد، أبعاد بالفرنية، ماكينة UTM، خريطة جهد الصدأ):

- `src/lib/evidence.ts` — الصور (بوقت/إحداثيات/جهاز/بصمة)، سجل المعدات ومعايرتها، العينات وسلسلة الحيازة، أوراق طرق القياس (ISO/IEC 17025 §6.4/§7.4/§7.6/§7.8).
- `src/ds/evidence.tsx` — `GeoMap` (Leaflet + OpenStreetMap: خريطة حقيقية بالمضلع/الجسات/النطاق الجغرافي)، `PhotoGrid` (معرض + بيانات الالتقاط)، `MethodCard`.
- `src/features/evidence/Evidence.tsx` — `/samples` (الأرشيف وسلسلة الحيازة + QR)، `/equipment` (المعايرة)، `TestSheet` (ورقة الاختبار المعتمدة داخل تفاصيل الطلب وصفحة المراجعة).
- **الصور في `public/evidence/` مأخوذة مؤقتاً من التقرير المرجعي في `References/` لأغراض العرض الداخلي — استبدلها بأصول العميل قبل أي عرض عام.**

## سيناريو العرض #1 — دورة اختبار قياسي كاملة في 5 دقائق

1. **المختبر** (`?as=u-lab`) → `/requests/TST-2026-049` — طلب مقبول وموعده المعتمد أمس → «بدء التنفيذ» → تسجيل استلام العينة (يُسجَّل بدء متأخر عن الموعد ويُخصم من الالتزام — B.R.149).
2. **المقاول** (`?as=u-cont`) → نفس الطلب → «تأكيد استلام العينة» (يبدأ عداد SLA، ويُسجَّل `confirmedAt`).
3. **المختبر** → إدخال النتائج والأبعاد والمعدات المعايرة والصور → «رفع النتيجة لاعتماد الاستشاري».
4. **الاستشاري** (`?as=u-cons`) → «اعتماد النتائج» → ورقة الاختبار والأدلة → اعتماد/رفض بسبب (الرفض يتيح للمقاول طلب إعادة).
5. بعد اعتماد كل المخرجات يكتمل الطلب تلقائياً وتصبح الفاتورة مستحقة؛ الاعتماد التلقائي بعد 48 س وانتهاء مهلة المختبر 12 س ينفّذهما `tick()` كل دقيقة.

> حراس المسارات: الوصول إلى `/admin/*`, `/catalog`, `/approvals`, `/equipment`… خارج صلاحيات الدور يُعاد توجيهه للوحة مع تسجيل حدث «محاولة وصول مرفوضة» في سجل التدقيق. قواعد الأعمال تُجمَّد لكل طلب عند إرساله (`request.rules`) — تعديل الإعدادات يسري على الطلبات الجديدة فقط.

## صورة صفحة الدخول

`public/brand/momrah-hero.jpg` — أفق الرياض (برج المملكة) من Wikimedia Commons، الملف «Beautiful Kingdom Tower.jpg» للمصوّر Ibuz4u، رخصة CC BY-SA 4.0 — يلزم إبقاء الإسناد عند النشر أو استبدالها بصورة من المكتبة الرسمية للوزارة. بديل عريض جاهز: `public/brand/riyadh-skyline.jpg` («Riyadh Skyline.jpg» للمصوّر B.alotaby، CC BY-SA 4.0).

## عمليات الإنشاء والتعديل (CRUD)

| الكيان | إنشاء | تعديل | إيقاف/حذف | أين |
|---|---|---|---|---|
| عرض سعر → عقد | المقاول من ملف المختبر «طلب عرض سعر» | المختبر يعدّل الأسعار النهائية (B.R.116) | رفض بسبب | `/quotes` — القبول يوقّع العقد بـ OTP ويُنشئه فعّالاً |
| طلب اختبار | `/requests/new` (مسودة/إرسال) | قبل الإرسال فقط `/requests/:id/edit` | إلغاء قبل قبول المختبر | `/requests` |
| قائمة الاختبارات | «إضافة اختبارات» من العناصر المرجعية | تعديل الوحدة/الطريقة/السعر | إيقاف مؤقت/تفعيل (لا حذف B.R.117) | `/catalog` |
| العناصر المرجعية | «اختبار مرجعي جديد» | تعديل الحقول والطرق | تعطيل | `/admin/reference` |
| المعدات والمعايرة | «إضافة معدّة» | تعديل + «تسجيل معايرة» | إخراج من الخدمة | `/equipment` |
| العينات | «تسجيل عينة» (ملصق QR) | أحداث الحيازة (نقل/استلام/تجهيز/اختبار/تخزين) | إتلاف بمحضر | `/samples` |
| الجسات/الطبقات/العينات الميدانية | إضافة جسة (استشاري/مختبر بمبرر)، إضافة طبقة، عينة | تعديل قبل الاكتمال | — | مسار الدراسة |
| التفويض | «تفويض جديد» مباشر/غير مباشر | تعديل الموظف المفوَّض | إلغاء بالتعديل (STS25) | `/delegations` |
| الموظفون | «إضافة موظف» + صلاحية التفويض | منح/سحب الصلاحية | — | `/profile` |
| المنشآت | تسجيل ذاتي `/register` أو «إضافة منشأة» للدعم | الملف التعريفي | إيقاف/تفعيل الحساب | `/admin/accounts` |
| التقييمات | المقاول بعد انتهاء العقد | — | إخفاء (الدعم) / حذف (مدير النظام) | `/directory/:id` · `/admin/ratings` |
| القوالب | «قالب جديد» (مسودة) | «إصدار جديد» نشر/مسودة | أرشفة تلقائية للإصدار السابق | `/admin/templates` |
| قاعدة المعرفة | «إصدار جديد (مسودة)» | — | اعتماد ونشر (حدث حرج) | `/admin/knowledge` |
| قواعد الأعمال | — | تعديل القيم (تسري على الجديد فقط) | — | `/admin/rules` |
| السياسات | «سياسة جديدة» | تعديل | حالة منتهٍ | `/governance/policies` |
| الفواتير | تلقائي عند القبول/الإرسال | — | سداد (سداد/مدى/تحويل) | `/invoices` |
| تذاكر الدعم | «فتح تذكرة» | — | — | `/help` |
